using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace JollyRoger.Data.BasicSerialization
{ 
    /// <summary>
    /// Basic Serializer for quick and dirty serialization of files.
    /// </summary>
    public class BasicSerializer : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}